{
  "name": "ts-love",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tstl -p tsconfig.json",
    "start": "npm run build && love build",
    "pack": "npm run build && cd build && zip -9 -r ../TypeScriptGame.love *"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript-to-lua": "^0.11.1"
  }
}
